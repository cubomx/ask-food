{
  "name": "ask-food",
  "version": "1.0.0",
  "description": "Application for asking food and don't spent time waiting",
  "main": "server/main.js",
  "scripts": {
    "start": "node server/main.js",
    "build": "cd client && npm install && npm run build"
  },
  "author": "Cubo",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  },
  "engines": {
    "node": "14.15.5"
  }
}
